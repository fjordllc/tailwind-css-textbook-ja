# Theme Assets

`mdbook-book.css` は、mdBook 向けの共通 UI スタイルです。

使い方:

1. 共通スタイルを `@import "./mdbook-book.css";` で読み込む
2. 各書籍の `theme/*.css` で色・余白・見出しサイズなどの変数を上書きする

このリポジトリでは `tailwind-css-textbook.css` が薄いラッパーになっています。
